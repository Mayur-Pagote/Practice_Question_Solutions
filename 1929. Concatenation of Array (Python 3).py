class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        temp = nums # Initializing temp variable with nums
        return temp+nums # Concatenating both the list and returning it
